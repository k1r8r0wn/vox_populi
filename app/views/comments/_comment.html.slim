li
  .header
    b = comment.user.username
    '  at
    b = comment.created_at.strftime('%d %B %Y | %I:%M %p %Z')
    '  said:
  .content
    p = comment.content

  - if current_user
    = link_to 'Add a Reply', new_comment_comment_path(comment), remote: true, id: "comment_#{comment.id}"

  - if comment.comments.any?
    ul
      = render(partial: 'comments/comment', collection: comment.comments)
